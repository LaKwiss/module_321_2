<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Connexion</title>
  </head>
  <body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 rounded shadow-md w-96">
      <h1 class="text-2xl mb-4 text-center">Connexion</h1>
      <% if (error) { %>
      <p class="text-red-500 text-center mb-4"><%= error %></p>
      <% } %>
      <a
        href="/auth/google"
        class="block bg-red-500 text-white p-2 rounded text-center mb-4"
      >
        Se connecter avec Google
      </a>
      <!-- Si vous souhaitez proposer une authentification locale ultérieurement, vous pouvez ajouter le formulaire ici -->
    </div>
  </body>
</html>
